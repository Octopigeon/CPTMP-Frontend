import { AutoAvatarPipe } from './auto-avatar.pipe';

describe('AutoAvatarPipe', () => {
  it('create an instance', () => {
    const pipe = new AutoAvatarPipe();
    expect(pipe).toBeTruthy();
  });
});
